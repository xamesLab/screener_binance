const WIDTH = 350;
const HEIGHT = 230;
const PADDING = 15;

export const conf = {
  WIDTH: WIDTH,
  //HEIGHT: HEIGHT,
  DPI_WIDTH: WIDTH * 2,
  //DPI_HEIGHT: HEIGHT * 2,
  ROWS_COUNT: 5,
  PADDING: PADDING,
  //VIEW_HEIGHT: HEIGHT * 2 - PADDING * 2,
  colors: {
    high: "green",
    low: "red",
  },
};
